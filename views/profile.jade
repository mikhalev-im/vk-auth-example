extends layout

block content
  h1= title
  ul
    each friend in friends
      li #{friend.first_name + ' '+ friend.last_name}
  form(method="GET" action="/vkontakte/logout")
    button.btn.btn-login(type = "submit") Logout
